const initialState = {
  profile: null,
  profiles: null,
  loading: false //this is for working with a spinner
};

//handle profile actions
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
